# job-fit

This repository was initialized as a starter Node.js project.

## Project setup

- `npm install` to install dependencies
- `node src/index.js` to run the starter script

## Notes

The repository was empty on clone, so a basic scaffold was created.
